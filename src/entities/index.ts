import { Auto } from "./auto.entity";
import { Book_session } from "./book_session.entity";
import { Rate } from "./rate.entity";

const entities = [Auto, Rate, Book_session];

export { Auto };
export { Rate };
export { Book_session };

export default entities;
